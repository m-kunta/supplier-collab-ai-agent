import { BriefingCreateForm } from "../../../components/BriefingCreateForm";

export default function NewBriefingPage() {
  return (
    <BriefingCreateForm
      heading="Generate Supplier Briefing"
      subheading="Run the pipeline and open a generated briefing with stream replay."
    />
  );
}
