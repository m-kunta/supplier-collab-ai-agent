python-dotenv>=1.0.0

# ---------------------------------------------------------------------------
# LLM Provider SDKs
# Install only the providers you intend to use.
# The agent gracefully falls back to ImportError when a package is absent.
# ---------------------------------------------------------------------------

# Anthropic (default provider — always install)
anthropic>=0.34.0

# OpenAI — uncomment to enable gpt-4o / gpt-4o-mini
# openai>=1.0.0

# Google Gen AI (Gemini 2.x) — uncomment to enable gemini-2.5-flash / pro
# Note: use 'google-genai', NOT the legacy 'google-generativeai'
# google-genai>=1.0.0

# Groq (ultra-low-latency open-source models) — uncomment to enable
# groq>=0.9.0

# ---------------------------------------------------------------------------
# Data Loading
# ---------------------------------------------------------------------------
PyYAML>=6.0.1
pandas>=2.0.0

# ---------------------------------------------------------------------------
# HTTP API (Phase 5)
# ---------------------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
google-genai==1.47.0
groq==1.0.0
openai==2.32.0

# ---------------------------------------------------------------------------
# Sprint 3 / Docx support
# ---------------------------------------------------------------------------
python-docx>=1.2.0
